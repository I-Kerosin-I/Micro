<UserControl x:Class="Micro.Views.RamWordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Micro.Views"
             xmlns:vm="clr-namespace:Micro.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:RamViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <DataGrid ItemsSource="{Binding MemoryWords, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False"
                  CanUserReorderColumns="False" 
                  CanUserResizeColumns="False" 
                  CanUserSortColumns="False" 
                  Margin="0,2,0,0" 
                  Grid.Row="0" CanUserResizeRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ад" Binding="{Binding Address}" Width="24" IsReadOnly="True" />
                <DataGridTextColumn Header="0" Binding="{Binding Cells[0].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="2" Binding="{Binding Cells[1].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="4" Binding="{Binding Cells[2].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="6" Binding="{Binding Cells[3].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="8" Binding="{Binding Cells[4].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="A" Binding="{Binding Cells[5].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="C" Binding="{Binding Cells[6].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>
                <DataGridTextColumn Header="E" Binding="{Binding Cells[7].Value, Converter={StaticResource ByteToHexConverter}, ConverterParameter=X4}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

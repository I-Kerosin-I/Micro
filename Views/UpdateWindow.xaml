<Window x:Class="Micro.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Micro.Views"
        xmlns:vm="clr-namespace:Micro.ViewModels"
        mc:Ignorable="d"
        Title="Доступна новая версия!" Height="450" Width="800" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=vm:UpdateViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.ColumnSpan="2" Text="{Binding ReleaseNotes}" ></TextBlock>

        <UniformGrid Grid.Column="1" Grid.Row="1" Columns="2" Margin="10,7,10,10">
            <Button Content="Обновить" Command="{Binding UpdateCommand}" Padding="0,0,0,0" HorizontalContentAlignment="Center" HorizontalAlignment="Center" MinWidth="130" />
            <Button Content="Напомнить позже" HorizontalAlignment="Center" MinWidth="130" IsCancel="True">

            </Button>
        </UniformGrid>
    </Grid>
</Window>
